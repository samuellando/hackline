import { describe, expect, test } from '@jest/globals';
import { ApiClient } from "$lib/Api";

describe('sum module', () => {
  test('adds 1 + 2 to equal 3', () => {
    expect(1 + 2).toBe(3);
  });
});
